[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
plotly = "*"
pandas = "*"

[dev-packages]
pytest = "*"
ipykernel = "*"
notebook = "*"
nbformat = "*"
ruff = "*"
# devenv/es-init parses ingex's index-template ConfigMaps; only its tests need
# this locally, since the script itself runs inside a container that has it.
pyyaml = "*"

[requires]
python_version = "3.13"
